messaging:
  host: ${rabbit.host}
  virtualHost: ${rabbit.vhost}
  username: ${rabbit.username}
  password: ${rabbit.password}
neo:
  neoRepository: ${neo.repository}
clb:
  serverName: "${checklistbank.db.host}"
  databaseName: "${checklistbank.db.name}"
  user: ${checklistbank.db.username}
  password: "${checklistbank.db.password}"
  maximumPoolSize: 10
solr:
  serverType: CLOUD
  #serverHome: prodmaster1-vh.gbif.org:2181,prodmaster2-vh.gbif.org:2181,prodmaster3-vh.gbif.org:2181/uatsolr
  collection: clb_import_test
zookeeper:
  connectionString: c1n1.gbif.org,c1n2.gbif.org,c1n3.gbif.org
  namespace: dev
